const $ = (s, p=document) => p.querySelector(s);
const state = { session:null, items:[], catalog:[], game:null, market:null, express:false };
const gameNames = {blox:'Blox Fruits',garden:'Grow a Garden'};
const esc = value => String(value ?? '').replace(/[&<>'"]/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
const safeNext = value => ['/inr.html','/inr-blox-fruits.html','/inr-grow-a-garden.html','/roblox-login.html'].includes(value) ? value : '/inr.html';
async function api(url, options={}) { const r=await fetch(url,{headers:{'Content-Type':'application/json',...(options.headers||{})},...options}); const data=await r.json().catch(()=>({error:'Unexpected server response.'})); if(!r.ok) throw new Error(data.error||'Something went wrong.'); return data; }
function toast(message){let e=$('.toast');if(!e){e=document.createElement('div');e.className='toast';document.body.append(e)}e.textContent=message;e.classList.remove('hidden');clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>e.classList.add('hidden'),3600)}
function money(value){return value==null?'Price on request':`₹${value.toLocaleString('en-IN')}`}
function nav(){const menu=$('#menu');const mobile=$('#mobileMenu'); if(!menu)return;const close=()=>{mobile.classList.remove('open');menu.setAttribute('aria-expanded','false');menu.setAttribute('aria-label','Open menu');menu.textContent='☰'};menu.onclick=()=>{if(mobile.classList.contains('open'))close();else{mobile.classList.add('open');menu.setAttribute('aria-expanded','true');menu.setAttribute('aria-label','Close menu');menu.textContent='×'}};mobile.querySelectorAll('a').forEach(a=>a.onclick=close);document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})}
function header(active=''){return `<a class="skip-link" href="#main">Skip to content</a><header><nav class="wrap" aria-label="Primary"><a class="brand" href="/"><span class="mark"><span>K</span></span>k3rxsene's Services</a><div class="navlinks"><a class="${active==='games'?'active':''}" href="/#games">Games</a><a class="${active==='inr'?'active':''}" href="/inr.html">INR access</a><a class="btn blue" href="${active==='inr'&&state.session?.inr?'/inr.html':'/inr-login.html'}">${state.session?.inr?'INR catalogue':'INR sign in'}</a><button class="menu" id="menu" aria-label="Open menu" aria-expanded="false">☰</button></div></nav></header><div class="mobile-menu" id="mobileMenu"><a href="/#games">Games</a><a href="/inr.html">INR access</a><a class="btn blue" href="${state.session?.inr?'/inr.html':'/inr-login.html'}">${state.session?.inr?'INR catalogue':'INR sign in'}</a></div>`}
function footer(){return `<footer class="site-footer wrap"><span class="footer-brand"><span class="mark" style="width:26px;height:26px"><span style="font-size:11px">K</span></span>k3rxsene's Services</span><span>© 2026 k3rxsene's Services. All rights reserved.</span><span>Secure ordering foundation</span></footer>`}
async function initSession(){try{state.session=await api('/api/session')}catch{state.session={inr:false,roblox:null,configured:{roblox:false}}}}
function renderHome(){document.body.innerHTML=`${header('games')}<main id="main"><section class="hero wrap"><div class="hero-copy"><p class="kicker"><span class="dot"></span>Secure Roblox services</p><h1>Your next game goal, <em>properly handled.</em></h1><p class="intro">Trusted game services with a clear catalogue, transparent ordering, and a dedicated INR experience for Indian customers.</p><div class="hero-actions"><a class="btn blue" href="#games">Choose a game</a><a class="btn ghost" href="/inr-login.html">INR customer access</a></div><ul class="trust"><li>Clear order summaries</li><li>Express is always optional</li><li>Roblox identity before checkout</li></ul></div><div class="hero-visual" aria-hidden="true"><div class="halo"></div><div class="orb"><b>K</b></div></div></section><section class="band"><div class="section wrap" id="games"><div class="section-head"><p class="kicker"><span class="dot"></span>Game catalogue</p><h2>Choose your game</h2><p>Each catalogue keeps the same reliable flow, with services tailored to the game.</p></div><div class="game-grid"><a class="game-card" href="/blox-fruits.html"><img src="/bloxfruits.png" alt="Blox Fruits"><h3>Blox Fruits</h3><p>Leveling, V4, raids, swords and more.</p><span class="btn blue">View services</span></a><a class="game-card" href="/grow-a-garden.html"><img src="/growagarden.png" alt="Grow a Garden"><h3>Grow a Garden</h3><p>Valuable seeds, pets, sprinklers and currency.</p><span class="btn blue">View services</span></a><article class="game-card" aria-label="More games coming soon"><span class="placeholder">?</span><h3>More games</h3><p>New catalogues are being prepared.</p><span class="btn ghost">Coming soon</span></article></div></div></section><section class="section wrap"><div class="feature-grid"><article class="feature"><h3>One clear checkout</h3><p>Build an order, see the precise subtotal, then decide whether Express service is worth adding.</p></article><article class="feature"><h3>INR access stays private</h3><p>Indian pricing is separated behind authentication rather than exposed through a public currency switch.</p></article><article class="feature"><h3>Designed to grow</h3><p>Games and services are catalogue-driven, so new offerings can be added without rebuilding the site.</p></article></div></section></main>${footer()}`;nav()}
function catalogMarkup(){const isInr=state.market==='inr';const heading=isInr?'INR service catalogue':`${gameNames[state.game]} services`;const theme=state.game==='garden'?'garden':'';document.body.className=theme;document.body.innerHTML=`${header(isInr?'inr':'games')}<main id="main"><section class="catalog-hero wrap"><p class="kicker"><span class="dot"></span>${isInr?'Authenticated INR access':gameNames[state.game]}</p><h1>${heading}</h1><p>${isInr?'Your approved INR catalogue. Add items to build an order; prices are verified by the server at submission.':'Browse the catalogue and request a rate. Add services to your request below — INR pricing and instant checkout are available after INR access is authenticated.'}</p></section><section class="wrap catalog-layout"><div><div class="notice" style="margin-bottom:16px"><strong>${state.game==='garden'?'Availability matters.':'Service scope matters.'}</strong> ${state.game==='garden'?'Grow a Garden market items are availability-based; an item marked “Price on request” cannot be checked out until a rate is agreed.':'Some services have account-specific scope; final delivery details are confirmed after checkout.'}</div><div class="service-grid" id="serviceGrid"></div></div><aside class="cart panel"><h2>${isInr?'Your order':'Your rate request'}</h2><ul class="cart-items" id="cartItems"></ul>${isInr?`<div class="cart-summary"><div><span>Subtotal</span><b id="subtotal">₹0</b></div><div><span id="expressLabel">Express Service: Not applied</span><b id="expressPrice">—</b></div><div><strong>Total</strong><strong id="total">₹0</strong></div></div><div class="express"><label><input type="checkbox" id="express"> <span>Add Express Service <small>(+₹${state.expressSurcharge})</small></span></label><span>One priority surcharge per order. It is never multiplied by service.</span></div><button class="btn blue" style="width:100%" id="checkout">Continue to Roblox login</button>`:`<div class="field"><label for="quoteEmail">Contact email</label><input id="quoteEmail" type="email" required placeholder="you@example.com"></div><div class="field"><label for="quoteNotes">Notes (optional)</label><input id="quoteNotes" maxlength="400" placeholder="Any details we should know"></div><div class="error" id="quoteError"></div><button class="btn blue" style="width:100%" id="requestQuote">Send rate request</button><div class="notice" style="margin-top:17px">Already have INR access? <a href="/inr-login.html" style="color:var(--accent2);font-weight:700">Sign in for live pricing</a></div>`}</aside></section></main>${footer()}<div class="modal" id="modal"></div>`;renderServices();renderCart();nav()}
function renderServices(){const grid=$('#serviceGrid');grid.innerHTML=state.catalog.map(item=>`<article class="service-card"><span class="tag">${item.tag||'Service'}</span><h3>${item.name}</h3><p>${item.description}</p><footer><span class="price ${item.price==null?'request':''}">${money(item.price)}</span><button class="btn ghost" data-add="${item.id}">Add</button></footer></article>`).join('');grid.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>addItem(b.dataset.add))}
function addItem(id){const item=state.catalog.find(x=>x.id===id);const existing=state.items.find(x=>x.id===id);if(existing)existing.quantity++;else state.items.push({...item,quantity:1});renderCart();toast(`${item.name} added.`)}
function renderCart(){const out=$('#cartItems');if(!out)return;const isInr=state.market==='inr';if(!state.items.length)out.innerHTML='<li class="cart-empty">No services selected yet.</li>';else out.innerHTML=state.items.map(i=>`<li class="cart-line"><span>${i.name}<small>Quantity ${i.quantity}${isInr?` · ${money(i.price)} each`:''}</small></span><span>${isInr?`<b>${money(i.price*i.quantity)}</b><br>`:''}<button aria-label="Remove ${i.name}" data-remove="${i.id}">Remove</button></span></li>`).join('');out.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{state.items=state.items.filter(i=>i.id!==b.dataset.remove);renderCart()});
  if(isInr){const subtotal=state.items.reduce((x,i)=>x+i.price*i.quantity,0), surcharge=state.express?state.expressSurcharge:0;$('#subtotal').textContent=money(subtotal);$('#total').textContent=money(subtotal+surcharge);$('#expressLabel').textContent=state.express?'Express Service: Applied':'Express Service: Not applied';$('#expressPrice').textContent=state.express?`+${money(surcharge)}`:'—';const box=$('#express');if(box){box.checked=state.express;box.onchange=()=>{state.express=box.checked;renderCart()};$('#checkout').onclick=checkout}}
  else{const btn=$('#requestQuote');if(btn)btn.onclick=requestQuote}}
function checkout(){if(!state.items.length)return toast('Add at least one service before continuing.');sessionStorage.setItem('k3order',JSON.stringify({game:state.game,items:state.items.map(i=>({id:i.id,quantity:i.quantity})),express:state.express}));location.href='/roblox-login.html'}
async function requestQuote(){const errEl=$('#quoteError');errEl.textContent='';if(!state.items.length)return errEl.textContent='Add at least one service before requesting a rate.';const email=$('#quoteEmail').value.trim();const btn=$('#requestQuote');btn.disabled=true;btn.textContent='Sending…';try{const r=await api('/api/quotes',{method:'POST',body:JSON.stringify({game:state.game,items:state.items.map(i=>({id:i.id,quantity:i.quantity})),email,notes:$('#quoteNotes').value})});state.items=[];renderCart();toast(`Request sent — reference ${r.id}. We'll reply by email with a rate.`)}catch(e){errEl.textContent=e.message}finally{btn.disabled=false;btn.textContent='Send rate request'}}
async function renderCatalog(game,market){await initSession();if(market==='inr'&&!state.session.inr){location.href='/inr-login.html?next='+encodeURIComponent(`/inr-${game==='blox'?'blox-fruits':'grow-a-garden'}.html`);return}state.game=game;state.market=market;try{const data=await api(`/api/catalog?game=${game}&market=${market||'global'}`);state.catalog=data.items;state.expressSurcharge=data.expressSurcharge||0;catalogMarkup()}catch(e){document.body.innerHTML=`${header()}<main class="auth-page"><section class="auth-card panel"><h1>Catalogue unavailable</h1><p>${e.message}</p><a class="btn blue" href="/">Back home</a></section></main>`;nav()}}
function renderInrLogin(){document.body.innerHTML=`${header('inr')}<main class="auth-page" id="main"><form class="auth-card panel" id="login"><p class="kicker"><span class="dot"></span>Private pricing</p><h1>INR customer access</h1><p>Use the access details supplied to you. There is no public registration.</p><div class="field"><label for="username">Username</label><input id="username" autocomplete="username" required></div><div class="field"><label for="password">Password</label><input id="password" type="password" autocomplete="current-password" required></div><div class="error" id="error"></div><button class="btn blue" style="width:100%">Continue</button></form></main>${footer()}`;nav();$('#login').onsubmit=async e=>{e.preventDefault();const b=$('#login button');b.disabled=true;b.textContent='Checking access…';try{await api('/api/inr/login',{method:'POST',body:JSON.stringify({username:$('#username').value,password:$('#password').value})});location.href=safeNext(new URLSearchParams(location.search).get('next'))}catch(err){$('#error').textContent=err.message;b.disabled=false;b.textContent='Continue'}}}
async function renderInr(){await initSession();if(!state.session.inr){location.href='/inr-login.html?next=/inr.html';return}document.body.innerHTML=`${header('inr')}<main id="main"><section class="catalog-hero wrap"><p class="kicker"><span class="dot"></span>Authenticated access</p><h1>Your INR catalogue</h1><p>Select a game to see its INR services and build a secure order. Pricing remains protected behind this session.</p></section><section class="wrap section" style="padding-top:10px"><div class="game-grid"><a class="game-card" href="/inr-blox-fruits.html"><img src="/bloxfruits.png" alt="Blox Fruits"><h3>Blox Fruits</h3><p>INR services and checkout.</p><span class="btn blue">View INR catalogue</span></a><a class="game-card" href="/inr-grow-a-garden.html"><img src="/growagarden.png" alt="Grow a Garden"><h3>Grow a Garden</h3><p>INR services and checkout.</p><span class="btn blue">View INR catalogue</span></a><article class="game-card"><span class="placeholder">?</span><h3>More games</h3><p>Coming soon.</p></article></div><button class="btn ghost" style="margin-top:25px" id="logout">Sign out of INR access</button></section></main>${footer()}`;nav();$('#logout').onclick=async()=>{await api('/api/logout',{method:'POST'});location.href='/'}}
async function renderRobloxLogin(){await initSession();const saved=JSON.parse(sessionStorage.getItem('k3order')||'null');if(!saved){location.href='/inr.html';return}try{const data=await api(`/api/catalog?game=${saved.game}&market=inr`);state.game=saved.game;state.market='inr';state.catalog=data.items;state.expressSurcharge=data.expressSurcharge}catch{location.href='/inr-login.html?next=/roblox-login.html';return}document.body.innerHTML=`${header('inr')}<main class="auth-page" id="main"><section class="auth-card panel"><p class="kicker"><span class="dot"></span>Secure checkout</p><h1>Connect your Roblox identity</h1><p>Before you can confirm an order, authenticate with Roblox. Your identity will be shown for review and only the necessary details are included with the order.</p><div id="identityArea"></div><div class="error" id="error"></div><button class="btn blue" style="width:100%" id="robloxAction">Connect with Roblox</button></section></main>${footer()}<div class="modal" id="modal"></div>`;nav();const action=$('#robloxAction');if(state.session.roblox){$('#identityArea').innerHTML=`<div class="identity"><b>${state.session.roblox.displayName}</b>@${state.session.roblox.username}<br><span>${state.session.roblox.email||'No email returned by provider'}</span></div>`;action.textContent='Review and confirm order';action.onclick=()=>reviewOrder(saved)}else{action.onclick=()=>{if(!state.session.configured.roblox)return toast('Roblox integration has not been configured yet.');location.href='/api/roblox/start'}}}
function reviewOrder(saved){const modal=$('#modal');modal.innerHTML=`<section class="modal-card panel"><button class="modal-close" aria-label="Close">×</button><p class="kicker"><span class="dot"></span>Review order</p><h2>Confirm your order</h2><div id="reviewLines"></div><div class="express"><strong>Express Service: ${saved.express?`Applied (+₹${state.expressSurcharge})`:'Not applied'}</strong></div><div class="error" id="reviewError"></div><button class="btn blue" style="width:100%" id="confirm">Submit secure order</button></section>`;modal.classList.add('show');$('.modal-close',modal).onclick=()=>modal.classList.remove('show');const found=saved.items.map(x=>({...state.catalog.find(i=>i.id===x.id),quantity:x.quantity}));const subtotal=found.reduce((x,i)=>x+i.price*i.quantity,0),sur=saved.express?state.expressSurcharge:0;$('#reviewLines').innerHTML=found.map(i=>`<div class="cart-line"><span>${i.name} ×${i.quantity}</span><b>${money(i.price*i.quantity)}</b></div>`).join('')+`<div class="cart-summary"><div><span>Subtotal</span><b>${money(subtotal)}</b></div><div><strong>Total</strong><strong>${money(subtotal+sur)}</strong></div></div>`;$('#confirm').onclick=async()=>{const b=$('#confirm');b.disabled=true;b.textContent='Submitting…';try{const r=await api('/api/orders',{method:'POST',body:JSON.stringify(saved)});sessionStorage.removeItem('k3order');modal.innerHTML=`<section class="modal-card panel"><p class="kicker"><span class="dot"></span>Order received</p><h2>Thank you — ${r.order.id}</h2><p>Your order total is ${money(r.order.total)}. We’ve securely recorded the request and will confirm fulfilment details with you.</p><a class="btn blue" href="/inr.html">Back to catalogue</a></section>`}catch(e){$('#reviewError').textContent=e.message;b.disabled=false;b.textContent='Submit secure order'}}}
async function boot(){const path=location.pathname; if(path==='/')return renderHome();if(path==='/blox-fruits.html')return renderCatalog('blox','');if(path==='/grow-a-garden.html')return renderCatalog('garden','');if(path==='/inr-blox-fruits.html')return renderCatalog('blox','inr');if(path==='/inr-grow-a-garden.html')return renderCatalog('garden','inr');if(path==='/inr-login.html')return renderInrLogin();if(path==='/inr.html')return renderInr();if(path==='/roblox-login.html')return renderRobloxLogin();location.href='/'}boot();